# Portfolio detail pages — working list

Index (`/portfolio`) is done: pixel-blast hero, framed carousel, side poem, global click sparks.
Detail pages render via `src/routes/portfolio/project.tsx` + per-project `src/routes/portfolio/content/<slug>.tsx`.
Now redesigning each detail page, one at a time. Goal: each page individually designed and "fancier", with real screenshots of the actual thing (screenshots live here, not on the index carousel).

## Order

- [ ] **stoat** — capture the real TUI + CLI (`stoat ls/up/ssh`, VM list). Terminal frames as the hero art.
- [ ] **engrammic** — claim/evidence graph visuals; pull from engrammic.ai. Links: site, research, github.
- [ ] **ocloak** — EvilEye (ogl, react-bits) integration for the anti-surveillance theme. RF/board imagery.
- [ ] **palpatine** — lean into the joke; the unhinged-advice output, star count.
- [ ] **veil** — drop-in npm API snippet, memory-graph. Links: github, npm.

## Also

- [ ] money-mesh has no repo link (dead `#` in the group card) — add a URL or drop it from the groups.
- [ ] After all detail pages: prune any unused old-index leftovers.

Delete this file once the pass is complete.
