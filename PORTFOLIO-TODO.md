# Portfolio detail pages — working list

Index (`/portfolio`) is done: pixel-blast hero, framed carousel, side poem, global click sparks.
Detail pages render via `src/routes/portfolio/project.tsx`; a slug in `custom-pages.ts` overrides the shared dossier.

## Order

- [x] **stoat** — terminal frames as the hero art.
- [x] **engrammic** — claim/evidence graph visuals, paper stock.
- [x] **ocloak** — dither hero, floating EvilEyes, trust-collapse diagram.
- [x] **palpatine** — star wars world.
- [x] **veil** — palimpsest hero with the recall lens; diagrams built from the real repo constants (FSRS curve, AIMD band, scorer weights, convergence thresholds).

## Also

- [ ] money-mesh has no repo link (dead `#` in the group card) — add a URL or drop it from the groups.
- [ ] Prune unused old-index leftovers, and `content/veil.tsx` now that the veil page writes its own body.

Delete this file once the two items above are done.
