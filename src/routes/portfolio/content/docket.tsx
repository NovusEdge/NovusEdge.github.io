import type { ProjectContent } from './types'

export const docket: ProjectContent = {
  lede: 'Docket tracks what an agent has settled, ruled out, and left open, so the next step can build on the reasoning already done.',
  sections: [],
}
